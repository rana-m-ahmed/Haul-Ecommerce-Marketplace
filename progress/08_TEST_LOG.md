# 08 — Test Log

Append a new entry every session. Don't overwrite history — this file is the record that QA actually ran continuously instead of getting saved for the end.

---

### [Sprint] — [Track] — [Date/session label]

**Command(s) run:**
```
(paste exact commands here)
```

**Result summary:**
```
(paste pass/fail counts, key output)
```

**Live verification performed (if applicable):**
(what you ran/clicked/screenshotted to confirm this works beyond automated tests)

---
