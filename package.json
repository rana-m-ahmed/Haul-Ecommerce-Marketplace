{
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"node tests/firestore/firestore.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "firebase-tools": "^13.30.0"
  }
}
